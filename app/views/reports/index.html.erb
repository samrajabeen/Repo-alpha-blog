<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>


<h1>Listing all Reports</h1>
<p>
	<%= link_to "Create new Report", new_report_path %>
</p>
<table>
	<tr>
		<th>Title</th>
		<th>Description</th>
	</tr>
	<% @reports.each do |report| %>
	<tr>
		<td><%= report.title %></td>
		<td><%= report.description %></td>
		<td><%= link_to 'Edit', edit_report_path(report) %></td>
		<td><%= link_to "Show Report" , report_path(report) %></td>
		<td><%= link_to "Delete" , report_path(report), method: :delete, data: {confirm: "Are you sure?" } %></td>
	</tr>
	<% end %>
</table>
</body>
</html>
